%h1 Listing collections

%h5 Dostępne kolekcje
%table
  %thead
    %tr
      %th Id
      %th Zakończenie
      %th
      %th

  %tbody
    - @public_collections.each do |collection|
      %tr
        %td= collection.id
        %td= link_to 'Show', creator_collection_path(collection)

%h5 Moja Kolekcja  
%table
  %thead
    %tr
      %th Id
      %th Zakończenie
      %th
      %th

  %tbody
    - @my_collection.each do |collection|
      %tr
        %td= collection.id
        %td= link_to 'Show', creator_collection_path(collection)