= form_for [:creator, @bracelet] do |f|
  - if @bracelet.errors.any?
    #error_explanation
      %h2= "#{pluralize(@bracelet.errors.count, "error")} prohibited this bracelet from being saved:"
      %ul
        - @bracelet.errors.full_messages.each do |msg|
          %li= msg
  = "W poprzednim kroku wybrałaś długość i szerokość bransoletki. Jeśli chcesz zmienić długość lub szerokość bransoletki kliknij tutaj. Wszystkie zmiany z tego widoku nie zostaną zapisane:"
  = link_to 'New Bracelet', new_creator_bracelet_path
  %table
    %tr
      %td{class: "fill-entire_table", style:'width:150px',onclick:"fillTable(#{@bracelet.id})"}
        %center
          = "wypelnij"
  = render 'first_throw'
  .tools{style:'margin-left:15px;display:table;float:left'}
    - (0..@bracelet.height).each do |row_index|
      %table
        %tr
          %td{class: "fill-row-#{row_index}", onclick:"fillRow(#{row_index}, #{@bracelet.id})"}

  = render 'pallete' 
  = render 'second_throw'
  .field
    = f.label :color_position_id
    = f.number_field :color_position_id
  .field
    = f.label :price
    = f.number_field :price
  .actions
    = f.submit 'Save'
