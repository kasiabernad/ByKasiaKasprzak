%tr{id: "cp-single-row-#{@row_index}"}
  - if ((@row_index % 2 ) == 0)
    %td{class:"no-border"}
  - @color_positions.each do |x|
    %td{onclick:"adchangeColor(#{x.id})", id: "cp-#{x.id}", style:"background-color:##{x.color.hex};", class: "cp-row-#{@row_index}"}
      - x.position